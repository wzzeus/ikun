# ============================================================================
# 鸡王争霸赛 - 生产环境变量配置模板
# 复制此文件为 .env.production 并填写实际值
# 使用: docker-compose -f docker-compose.prod.yml --env-file .env.production up -d
# ============================================================================

# ==========================================================================
# MySQL 数据库配置
# ==========================================================================
MYSQL_ROOT_PASSWORD=your_strong_root_password_here
MYSQL_DATABASE=chicken_king
MYSQL_USER=chicken
MYSQL_PASSWORD=your_strong_db_password_here

# ==========================================================================
# 后端应用配置
# ==========================================================================
# 重要：生产环境必须使用强随机密钥
# 生成方式: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_64_char_random_secret_key_here

# 前端地址（用于 OAuth 回调和 CORS）
FRONTEND_URL=https://your-domain.com

# ==========================================================================
# Linux.do OAuth2 配置
# 在 https://connect.linux.do 创建应用获取
# ==========================================================================
LINUX_DO_CLIENT_ID=your_linux_do_client_id
LINUX_DO_CLIENT_SECRET=your_linux_do_client_secret
# 回调地址必须与 Linux.do 后台配置一致
LINUX_DO_REDIRECT_URI=https://your-domain.com/api/v1/auth/linuxdo/callback

# ==========================================================================
# GitHub OAuth2 配置
# 在 https://github.com/settings/developers 创建 OAuth App 获取
# ==========================================================================
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_REDIRECT_URI=https://your-domain.com/api/v1/auth/github/callback

# ==========================================================================
# 前端构建配置
# ==========================================================================
# 生产环境使用相对路径，通过 Nginx 代理
VITE_API_URL=/api/v1

# ==========================================================================
# Umami 统计分析配置 (可选)
# 如果不需要，可以不填或删除 umami 相关服务
# ==========================================================================
UMAMI_APP_SECRET=your_umami_app_secret_here
UMAMI_DB_NAME=umami
UMAMI_DB_USER=umami
UMAMI_DB_PASSWORD=your_umami_db_password

# ==========================================================================
# 其他可选配置
# ==========================================================================
# GitHub Token（提高 API 速率限制）
# GITHUB_TOKEN=your_github_personal_access_token
